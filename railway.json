{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/cloudbath/railway/Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -lc 'node /app/openclaw.mjs config set gateway.controlUi.allowedOrigins \"[\\\"https://cloudbath-openclaw-production.up.railway.app\\\"]\" --strict-json && exec node /app/openclaw.mjs gateway --allow-unconfigured --bind lan --port \"${PORT:-8080}\"'",
    "healthcheckPath": "/readyz",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
